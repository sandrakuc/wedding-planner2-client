import { WeddingOutfitCriteria } from './wedding-outfit-criteria';

describe('WeddingOutfitCriteria', () => {
  it('should create an instance', () => {
    expect(new WeddingOutfitCriteria()).toBeTruthy();
  });
});
