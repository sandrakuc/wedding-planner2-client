import { WeddingServicesSet } from './wedding-services-set';

describe('WeddingServicesSet', () => {
  it('should create an instance', () => {
    expect(new WeddingServicesSet()).toBeTruthy();
  });
});
