import { WeddingSuiteStore } from './wedding-suite-store';

describe('WeddingSuiteStore', () => {
  it('should create an instance', () => {
    expect(new WeddingSuiteStore()).toBeTruthy();
  });
});
