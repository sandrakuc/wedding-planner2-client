import { CarRentingService } from './car-renting-service';

describe('CarRentingService', () => {
  it('should create an instance', () => {
    expect(new CarRentingService()).toBeTruthy();
  });
});
